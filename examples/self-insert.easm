; Дескрипторы для операндов будущей команды mov 42, [10].
mov 2, [0]
mov 42, [1]
mov 1, [2]
mov 10, [3]

; Вставляем mov (opcode 19) в конец. Старая следующая команда — jmp.
insert 19, 7, 0, 2
jmp 7
halt
